create table  users (
u_id integer not NULL AUTO_INCREMENT,
username varchar(50) character set utf8,
password varchar(100) character set utf8,
userrole varchar(30) character set utf8,
primary key (u_id)
);
